import { NumberordecimalsDirective } from './numberordecimals.directive';

describe('NumberordecimalsDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberordecimalsDirective();
    expect(directive).toBeTruthy();
  });
});
